import { Models } from "./models.model";

/**
 * Describe an organisation 
 */
export class Organisation extends Models {
    /**
     * Load data into model
     * @param model 
     */
    constructor(model: any) {
        super(model)
    }
}
