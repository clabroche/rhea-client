import { Models } from './models.model'
/**
 * Describe a Structure
 */
export class Structure extends Models {
  /**
   * Load data into model
   * @param model
   */
  constructor(model: any) {
    super(model)
  }
}
