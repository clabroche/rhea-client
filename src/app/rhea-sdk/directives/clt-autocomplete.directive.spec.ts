import { CltAutocompleteDirective } from './clt-autocomplete.directive';

describe('CltAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new CltAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
