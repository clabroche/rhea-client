import { CltResizeTextAreaDirective } from './clt-resize-text-area.directive';

describe('CltResizeTextAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new CltResizeTextAreaDirective();
    expect(directive).toBeTruthy();
  });
});
